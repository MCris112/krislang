print("=== BASIC FUNCTION CALL ===");
VOID hello(STRING $name) {
    print("Hello " + $name);
}
hello("Bianca");

print("=== MULTIPLE PARAMETERS ===");
VOID greet(STRING $a, STRING $b) {
    print($a + " " + $b);
}
greet("Hello", "World");

print("=== TYPE MISMATCH (should error) ===");
VOID testInt(INT $x) {
    print("X = " + $x);
}
testInt("not an int");   // expect type error
